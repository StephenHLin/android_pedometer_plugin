to run:

set ANDROID_HOME=C:\Users\9s\AppData\Local\Android\Sdk
gradlew assemble


need to point it at the right sdk, ai too lazy to set env var
gradle is used to compile the android side

the rest i dont know because ai bullshit